module SKMD;

fun main(): void {
  acc = mutable Vector[];
  loop {
    read_line() match {
    | None() ->
      _ = debug(parse(acc.join("")).collect(Array));
      return void
    | Some(str) ->
      acc.push(str);
      acc.push("\n")
    }
  };
}
