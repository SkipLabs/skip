{
  "name": "@skipruntime/wasm",
  "version": "0.0.3",
  "type": "module",
  "scripts": {
    "build": "skargo build -r --target wasm32-unknown-unknown --lib --manifest-path=./Skargo.toml --out-dir=../wasm/dist/src/ && skargo build --lib --manifest-path=./Skargo.toml --out-dir=$HOME/skip-runtime/",
    "clean": "skargo clean"
  }
}
