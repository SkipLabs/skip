{
  "name": "@skipruntime/wasm",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "skargo build -r --target wasm32-unknown-unknown --lib --manifest-path=./Skargo.toml --out-dir=../wasm/dist/src/",
    "clean": "skargo clean"
  }
}
