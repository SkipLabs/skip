{
  "name": "@skipruntime/skipper-helpers",
  "version": "0.0.2",
  "type": "module",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "scripts": {
    "build-alt": "tsc --outDir ./publish",
    "clean-alt": "rm -rf ./publish/tsconfig.tsbuildinfo ./publish/src",
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo && npm run clean-alt",
    "lint": "eslint"
  },
  "dependencies": {
    "@skipruntime/core": "0.0.24",
    "eventsource": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@skiplabs/eslint-config": "^0.0.2",
    "@skiplabs/tsconfig": "^0.0.3"
  }
}
