module SKFSTest;

fun getAllTests(): SortedMap<String, () ~> mutable SKFS.Context> {
  SortedMap[
    "subDir" => testSubDir,
    "interpretor" => testInterpretor,
    "lazyUnit" => testLazyUnit,
    //    "subDirUnit" => testSubDirUnit,
    //    "subsubDirUnit" => testSubSubDirUnit,
  ]
}
