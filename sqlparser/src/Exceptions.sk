module SQLParser;

base class Error extends Exception

base class TokenizerError extends Error {
  children =
  | InvalidTokenError(pos: Int, msg: String)
}

base class ParserError extends Error {
  children =
  | NotImplementedError(pos: Int)
  | UnexpectedTokenError(pos: Int, msg: String)
}
