module alias T = SKTest;

module Utils;

fun version(str: String): Semver.Version {
  Semver.Version::fromString(str)
}

fun req(str: String): Semver.VersionReq {
  Semver.VersionReq::fromString(str)
}

fun comparator(str: String): Semver.Comparator {
  Semver.Comparator::fromString(str)
}

module end;
