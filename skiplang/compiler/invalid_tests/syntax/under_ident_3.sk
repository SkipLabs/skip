base class T {
  children =
  | T1(x: Int, y: Int, z: Int)
  | T2(x: Int, y: Int, z: String)

  fun broken(): Int {
    this match {
    | T1(_x, _y, _)
    | T2(_x, _y, _) ->
      _x + _y
    }
  }
}

fun main(): void {
  t2 = T2(1, 2, "three");
  print_string(t2.broken().toString())
}
