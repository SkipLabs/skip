module Typechecking_Exhaustiveness_NestedWildcardConstraints;
fun test<T>(x: (?T, ?T)): void {
  x match {
  | (Some _, None())
  | (None(), None())
  | (None(), _)
  | (Some(_), Some(_)) ->
    void
  }
}

@exptest
fun main(): void {
  print_string("Pass")
}
module end;
