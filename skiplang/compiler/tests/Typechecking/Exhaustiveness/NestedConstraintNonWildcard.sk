module Typechecking_Exhaustiveness_NestedConstraintNonWildcard;
base class R {
  children =
  | L()
  | T()
}

base class F {
  children =
  | A(R)
}

@exptest
fun main(): void {
  print_raw(
    (A(L()), void) match {
    | (A(L()), _) -> "a"
    | (A _, _) -> "b"
    },
  )
}
module end;
