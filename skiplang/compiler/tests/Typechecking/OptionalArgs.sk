module Typechecking_OptionalArgs;
fun test(x: Int, b: Bool = true, y: Int = 44): Int {
  if (b) y else x
}

@exptest
fun main(): void {
  print_raw(if (test(0) == 44) "Passed\n" else "FAIL")
}
module end;
