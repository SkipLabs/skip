fun main(): void {
  print_raw(
    try {
      f = x -> {
        if (x == 0) {
          return "Pass\n"
        } else {
          return "Fail"
        }
      };
      f(0)
    } catch {
    | _ -> "Fail"
    }
  )
}
