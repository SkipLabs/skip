module Runtime_LambdaOptimizeReturnOfBindLambda;
fun main(): void {
  lambda = () -> {
    s = "Fail";
    lam = () -> {
      !s = "Pass\n"
    };
    lam();
    s
  };
  print_raw(lambda());
}
module end;
