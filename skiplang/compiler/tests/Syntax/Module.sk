module Syntax_Module;
fun main(): void {
  value = Syntax_ModuleX.get() + SM_X().get() + Syntax_ModuleX.Y().get();
  print_raw(if (value == 11) "Pass\n" else "FAILED")
}

module end;

module Syntax_ModuleX;

class .SM_X() {
  fun get(): Int {
    1
  }
}

class Y() {
  fun get(): Int {
    10
  }
}

fun get(): Int {
  0
}

module end;
