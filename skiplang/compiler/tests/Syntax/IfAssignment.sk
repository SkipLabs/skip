module Syntax_IfAssignment;
fun f(c: Bool): String {
  value = "FAIL";
  if (c) !value = "Pa" else !value = "ss";
  value;
}

@exptest
fun main(): void {
  print_string(f(true) + f(false));
}
module end;
