/*
   A class that wraps a value of type T. This is used to make any type
   a pointer type, so that it can be passed to C runtime functions expecting
   objects.

   This class must not be a value class.
 */
class Box<T>(value: T) {
  fun unbox(): T {
    this.value
  }
}
