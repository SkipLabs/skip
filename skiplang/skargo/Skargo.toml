[package]
name = "skargo"
version = "0.3.5"

[dependencies]
std = { path = "../prelude" }
toml = { path = "../toml" }
cli = { path = "../cli" }
semver = { path = "../semver" }

[dev-dependencies]
sktest = { path = "../sktest" }

[[bin]]
name = "skargo"
main = "Skargo.main"
