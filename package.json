{
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@skiplabs/eslint-config": "^0.0.1",
    "@skiplabs/tsconfig": "^0.0.1",
    "eslint": "9.15.0",
    "prettier": "^3.4.1",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "pnpm --recursive run build --if-present",
    "clean": "pnpm --recursive run clean --if-present",
    "lint": "pnpm --recursive run lint --if-present",
    "test": "pnpm --recursive run test --if-present"
  }
}
